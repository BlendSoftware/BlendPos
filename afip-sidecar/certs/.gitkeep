# This file ensures the certs/ directory exists in the repository
# Real certificates are loaded from environment variables (AFIP_CERT_B64, AFIP_KEY_B64)
# and written to /certs/ at container startup by entrypoint.sh
