# ─────────────────────────────────────────────────────────────────────────────
# BlendPOS — AFIP Sidecar — Variables de entorno (EXAMPLE)
# Copiar a .env y ajustar valores.
# ─────────────────────────────────────────────────────────────────────────────

# CUIT del emisor de comprobantes
AFIP_CUIT_EMISOR=20442477060

# Certificado y clave privada
# En Docker se montan en /certs via docker-compose volume
# En local, usar rutas absolutas o relativas a los archivos .crt y .key
AFIP_CERT_PATH=/certs/afip.crt
AFIP_KEY_PATH=/certs/afip.key

# true = Homologación (testing AFIP) | false = Producción
AFIP_HOMOLOGACION=true

# Puerto del servidor
AFIP_PORT=8001

# Directorio de caché para tokens AFIP
AFIP_CACHE_DIR=/tmp/afip_cache

# Nivel de logging: DEBUG | INFO | WARN | ERROR
LOG_LEVEL=INFO
