// TODO Phase 3 — POS (SalePanel + ProductSearch + CartGrid + CashDrawer)
export default function POSPage() {
  return <div className="flex items-center justify-center h-screen"><p className="text-muted-foreground">POS — Phase 3</p></div>
}
