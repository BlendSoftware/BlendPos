// TODO Phase 3 — CashDrawer: payment modal (efectivo, debito, credito, transferencia)
// Invariant: NEVER show monto_esperado before cashier declares
export function CashDrawer() {
  return <div>CashDrawer — Phase 3</div>
}
