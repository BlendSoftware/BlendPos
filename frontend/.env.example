# ─────────────────────────────────────────────────────────────────────────────
# BlendPOS — Variables de entorno
# Copiar este archivo a .env y ajustar los valores.
# ─────────────────────────────────────────────────────────────────────────────

# URL base del backend Go (sin barra al final)
# Usada por: api/client.ts, useAuthStore.ts, PosTerminal.tsx
VITE_API_BASE=http://localhost:8000

# Usada por: services/api/http.ts, ventas.ts, sales.ts, facturacion.ts, proveedores.ts, useCajaStore.ts
VITE_API_URL=http://localhost:8000

# Baud rate de la impresora térmica via Web Serial API
VITE_PRINTER_BAUD_RATE=9600
